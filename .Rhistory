# Prepare a preprocessing recipe.
(ifood_recipe <- training(ifood_split) |>
recipe(Response ~ Income + Recency + Kidhome) |>
step_dummy(Kidhome) |>
prep())
ifood |>
mutate(Kidhome = as.character(Kidhome),
Response = as.factor(Response))
ifood <- ifood |>
mutate(Kidhome = as.character(Kidhome),
Response = as.factor(Response))
# do we need to add strata?
ifood_split <- initial_split(data = ifood, prop = 0.9)
training(ifood_split)
# Prepare a preprocessing recipe.
(ifood_recipe <- training(ifood_split) |>
recipe(Response ~ Income + Recency + Kidhome) |>
step_dummy(Kidhome) |>
prep())
baked_training <- ifood_recipe |>
bake(training(ifood_split))
baked_training <- ifood_recipe |>
bake(training(ifood_split))
baked_testing <- ifood_recipe |>
bake(testing(ifood_split))
# Fit a logistic regression.
fit <- logistic_reg() |>
set_engine("glm") |>
fit(Response ~ ., data = baked_training)
fit |>
predict(new_data = baked_testing)
# Compute model accuracy.
fit |>
predict(new_data = baked_testing)|>
bind_cols(baked_testing) |>
accuracy(truth = Response, estimate = .pred_class)
fit |>
predict(new_data = baked_testing)|>
bind_cols(baked_testing)
(ifood <- read_csv("ifood.csv"))
ifood <- ifood |>
drop_na() |>
mutate(Kidhome = as.character(Kidhome),
Response = as.factor(Response))
# do we need to add strata?
ifood_split <- initial_split(data = ifood, prop = 0.9)
# Prepare a preprocessing recipe.
(ifood_recipe <- training(ifood_split) |>
recipe(Response ~ Income + Recency + Kidhome) |>
step_dummy(Kidhome) |>
prep())
baked_training <- ifood_recipe |>
bake(training(ifood_split))
baked_testing <- ifood_recipe |>
bake(testing(ifood_split))
# Fit a logistic regression.
fit <- logistic_reg() |>
set_engine("glm") |>
fit(Response ~ ., data = baked_training)
# Compute model accuracy.
fit |>
predict(new_data = baked_testing)|>
bind_cols(baked_testing) |>
accuracy(truth = Response, estimate = .pred_class)
fit |>
predict(new_data = baked_testing)
# Compute model accuracy.
fit |>
predict(new_data = baked_testing)|>
bind_cols(baked_testing) |>
accuracy(truth = Response, estimate = .pred_class)
ifood
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome, y = Reponse)) +
geom_col()
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome, y = Response)) +
geom_col()
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome, y = Response)) +
geom_col(position = 'fill')
summary(ifood)
(ifood <- read_csv("ifood.csv"))
ifood <- ifood |>
drop_na()
summary(ifood)
ifood |>
filter(Year_Birth < 1959 | Income > 100000)
ifood |>
filter(Year_Birth < 1920 | Income > 100000)
ifood |>
arrange(Year_Birth)
ifood |>
mutate(Year_Birth = if_else(Year_Birth < 1940, 1940, Year_Birth))
ifood <- ifood |>
mutate(Year_Birth = if_else(Year_Birth < 1940, 1940, Year_Birth))
ifood |>
arrange(desc(Income))
ifood <- ifood |>
mutate(Income = if_else(Income == 666666, 162397, Income))
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome, fill = Reponse)) +
geom_bar()
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome, fill = Response)) +
geom_bar()
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome)) +
geom_bar(fill = Response)
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome, color = Response)) +
geom_bar()
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome, fill = as.factor(Response))) +
geom_bar()
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome, fill = as.factor(Response))) +
geom_bar(position = 'fill')
View(ifood)
ifood <- ifood |>
mutate(Kidhome = as.character(Kidhome),
Response = as.factor(Response))
ifood_split <- initial_split(data = ifood, prop = 0.9, strata = Response)
ifood_split <- initial_split(data = ifood, prop = 0.85, strata = Response)
# Prepare a preprocessing recipe.
(ifood_recipe <- training(ifood_split) |>
recipe(Response ~ Income + Recency + Kidhome) |>
step_dummy(Kidhome) |>
prep())
# Prepare a preprocessing recipe.
(ifood_recipe <- training(ifood_split) |>
recipe(Response ~ Income + Recency + Kidhome + Complain) |>
step_dummy(Kidhome) |>
prep())
baked_training <- ifood_recipe |>
bake(training(ifood_split))
baked_testing <- ifood_recipe |>
bake(testing(ifood_split))
# Fit a logistic regression.
fit <- logistic_reg() |>
set_engine("glm") |>
fit(Response ~ ., data = baked_training)
# Compute model accuracy.
fit |>
predict(new_data = baked_testing)|>
bind_cols(baked_testing) |>
accuracy(truth = Response, estimate = .pred_class)
baked_testing
baked_testing |>
summarize(prop_no_response = mean(Response == 0))
# Confusion matrix.
fit |>
predict(new_data = baked_testing) |>
bind_cols(baked_testing) |>
conf_mat(truth = Response, estimate = .pred_class)
threshold <- 0.35
# Compute model accuracy.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
accuracy(truth = Response, estimate = pred_y)
# Confusion matrix.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
conf_mat(truth = Response, estimate = pred_y)
threshold <- 0.25
# Compute model accuracy.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
accuracy(truth = Response, estimate = pred_y)
# Confusion matrix.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
conf_mat(truth = Response, estimate = pred_y)
threshold <- 0.45
# Compute model accuracy.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
accuracy(truth = Response, estimate = pred_y)
# Confusion matrix.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
conf_mat(truth = Response, estimate = pred_y)
threshold <- 0.40
# Compute model accuracy.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
accuracy(truth = Response, estimate = pred_y)
threshold <- 0.40
# Compute model accuracy.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
accuracy(truth = Response, estimate = pred_y)
# Confusion matrix.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
conf_mat(truth = Response, estimate = pred_y)
# Prepare a preprocessing recipe.
(ifood_recipe <- training(ifood_split) |>
recipe(Response ~ Income + Recency + Kidhome + Complain +
AcceptedCmp1 + AcceptedCmp2 + AcceptedCmp3 +
AcceptedCmp4 + AcceptedCmp5) |>
step_dummy(Kidhome) |>
prep())
baked_training <- ifood_recipe |>
bake(training(ifood_split))
baked_testing <- ifood_recipe |>
bake(testing(ifood_split))
# Fit a logistic regression.
fit <- logistic_reg() |>
set_engine("glm") |>
fit(Response ~ ., data = baked_training)
# Compute model accuracy.
fit |>
predict(new_data = baked_testing)|>
bind_cols(baked_testing) |>
accuracy(truth = Response, estimate = .pred_class)
fit |>
tidy(conf.int = TRUE)
(ifood <- read_csv("ifood.csv"))
ifood <- ifood |>
drop_na()
summary(ifood)
ifood |>
arrange(Year_Birth)
ifood <- ifood |>
mutate(Year_Birth = if_else(Year_Birth < 1940, 1940, Year_Birth))
ifood |>
arrange(desc(Income))
ifood <- ifood |>
mutate(Income = if_else(Income == 666666, 162397, Income))
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome, fill = as.factor(Response))) +
geom_bar(position = 'fill')
ifood <- ifood |>
mutate(Kidhome = as.character(Kidhome),
Response = as.factor(Response))
ifood_split <- initial_split(data = ifood, prop = 0.85, strata = Response)
# Prepare a preprocessing recipe.
(ifood_recipe <- training(ifood_split) |>
recipe(Response ~ Income + Recency + Kidhome + Complain) |>
step_dummy(Kidhome) |>
prep())
baked_training <- ifood_recipe |>
bake(training(ifood_split))
baked_testing <- ifood_recipe |>
bake(testing(ifood_split))
# Fit a logistic regression.
fit <- logistic_reg() |>
set_engine("glm") |>
fit(Response ~ ., data = baked_training)
# Compute model accuracy.
fit |>
predict(new_data = baked_testing)|>
bind_cols(baked_testing) |>
accuracy(truth = Response, estimate = .pred_class)
baked_testing |>
summarize(prop_no_response = mean(Response == 0))
# Confusion matrix.
fit |>
predict(new_data = baked_testing) |>
bind_cols(baked_testing) |>
conf_mat(truth = Response, estimate = .pred_class)
threshold <- 0.40
# Compute model accuracy.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
accuracy(truth = Response, estimate = pred_y)
# Confusion matrix.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
conf_mat(truth = Response, estimate = pred_y)
# Prepare a preprocessing recipe.
(ifood_recipe <- training(ifood_split) |>
recipe(Response ~ Income + Recency + Kidhome + Complain +
AcceptedCmp1 + AcceptedCmp2 + AcceptedCmp3 +
AcceptedCmp4 + AcceptedCmp5) |>
step_dummy(Kidhome) |>
prep())
baked_training <- ifood_recipe |>
bake(training(ifood_split))
baked_testing <- ifood_recipe |>
bake(testing(ifood_split))
# Fit a logistic regression.
fit <- logistic_reg() |>
set_engine("glm") |>
fit(Response ~ ., data = baked_training)
# Compute model accuracy.
fit |>
predict(new_data = baked_testing)|>
bind_cols(baked_testing) |>
accuracy(truth = Response, estimate = .pred_class)
fit |>
tidy(conf.int = TRUE)
(ifood <- read_csv("ifood.csv"))
library(tidyverse)
(ifood <- read_csv("ifood.csv"))
ifood <- ifood |>
drop_na()
summary(ifood)
ifood |>
arrange(Year_Birth)
ifood <- ifood |>
mutate(Year_Birth = if_else(Year_Birth < 1940, 1940, Year_Birth))
ifood |>
arrange(desc(Income))
ifood <- ifood |>
mutate(Income = if_else(Income == 666666, 162397, Income))
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome, fill = as.factor(Response))) +
geom_bar(position = 'fill')
ifood <- ifood |>
mutate(Kidhome = as.character(Kidhome),
Response = as.factor(Response))
ifood_split <- initial_split(data = ifood, prop = 0.85, strata = Response)
library(tidyverse)
library(tidymodels)
(ifood <- read_csv("ifood.csv"))
ifood <- ifood |>
drop_na()
summary(ifood)
ifood |>
arrange(Year_Birth)
ifood <- ifood |>
mutate(Year_Birth = if_else(Year_Birth < 1940, 1940, Year_Birth))
ifood |>
arrange(desc(Income))
ifood <- ifood |>
mutate(Income = if_else(Income == 666666, 162397, Income))
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome, fill = as.factor(Response))) +
geom_bar(position = 'fill')
ifood <- ifood |>
mutate(Kidhome = as.character(Kidhome),
Response = as.factor(Response))
ifood_split <- initial_split(data = ifood, prop = 0.85, strata = Response)
# Prepare a preprocessing recipe.
(ifood_recipe <- training(ifood_split) |>
recipe(Response ~ Income + Recency + Kidhome + Complain) |>
step_dummy(Kidhome) |>
prep())
baked_training <- ifood_recipe |>
bake(training(ifood_split))
baked_testing <- ifood_recipe |>
bake(testing(ifood_split))
# Fit a logistic regression.
fit <- logistic_reg() |>
set_engine("glm") |>
fit(Response ~ ., data = baked_training)
# Compute model accuracy.
fit |>
predict(new_data = baked_testing)|>
bind_cols(baked_testing) |>
accuracy(truth = Response, estimate = .pred_class)
baked_testing |>
summarize(prop_no_response = mean(Response == 0))
# Confusion matrix.
fit |>
predict(new_data = baked_testing) |>
bind_cols(baked_testing) |>
conf_mat(truth = Response, estimate = .pred_class)
threshold <- 0.40
# Compute model accuracy.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
accuracy(truth = Response, estimate = pred_y)
# Confusion matrix.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
conf_mat(truth = Response, estimate = pred_y)
# Prepare a preprocessing recipe.
(ifood_recipe <- training(ifood_split) |>
recipe(Response ~ Income + Recency + Kidhome + Complain +
AcceptedCmp1 + AcceptedCmp2 + AcceptedCmp3 +
AcceptedCmp4 + AcceptedCmp5) |>
step_dummy(Kidhome) |>
prep())
baked_training <- ifood_recipe |>
bake(training(ifood_split))
baked_testing <- ifood_recipe |>
bake(testing(ifood_split))
# Fit a logistic regression.
fit <- logistic_reg() |>
set_engine("glm") |>
fit(Response ~ ., data = baked_training)
# Compute model accuracy.
fit |>
predict(new_data = baked_testing)|>
bind_cols(baked_testing) |>
accuracy(truth = Response, estimate = .pred_class)
fit |>
tidy(conf.int = TRUE)
library(tidyverse)
library(tidymodels)
(ifood <- read_csv("ifood.csv"))
ifood <- ifood |>
drop_na()
summary(ifood)
ifood |>
arrange(Year_Birth)
ifood <- ifood |>
mutate(Year_Birth = if_else(Year_Birth < 1940, 1940, Year_Birth))
ifood |>
arrange(desc(Income))
ifood <- ifood |>
mutate(Income = if_else(Income == 666666, 162397, Income))
ifood |>
mutate(Kidhome = as.character(Kidhome)) |>
ggplot(aes(x = Kidhome, fill = as.factor(Response))) +
geom_bar(position = 'fill')
ifood <- ifood |>
mutate(Kidhome = as.character(Kidhome),
Response = as.factor(Response))
set.seed(42)
ifood_split <- initial_split(data = ifood, prop = 0.85, strata = Response)
# Prepare a preprocessing recipe.
(ifood_recipe <- training(ifood_split) |>
recipe(Response ~ Income + Recency + Kidhome + Complain) |>
step_dummy(Kidhome) |>
prep())
baked_training <- ifood_recipe |>
bake(training(ifood_split))
baked_testing <- ifood_recipe |>
bake(testing(ifood_split))
# Fit a logistic regression.
fit <- logistic_reg() |>
set_engine("glm") |>
fit(Response ~ ., data = baked_training)
# Compute model accuracy.
fit |>
predict(new_data = baked_testing)|>
bind_cols(baked_testing) |>
accuracy(truth = Response, estimate = .pred_class)
baked_testing |>
summarize(prop_no_response = mean(Response == 0))
# Confusion matrix.
fit |>
predict(new_data = baked_testing) |>
bind_cols(baked_testing) |>
conf_mat(truth = Response, estimate = .pred_class)
threshold <- 0.40
# Compute model accuracy.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
accuracy(truth = Response, estimate = pred_y)
# Confusion matrix.
fit |>
predict(new_data = baked_testing, type = "prob") |>
bind_cols(baked_testing) |>
mutate(pred_y = if_else(.pred_1 >= threshold, 1, 0) |> factor()) |>
conf_mat(truth = Response, estimate = pred_y)
# Prepare a preprocessing recipe.
(ifood_recipe <- training(ifood_split) |>
recipe(Response ~ Income + Recency + Kidhome + Complain +
AcceptedCmp1 + AcceptedCmp2 + AcceptedCmp3 +
AcceptedCmp4 + AcceptedCmp5) |>
step_dummy(Kidhome) |>
prep())
baked_training <- ifood_recipe |>
bake(training(ifood_split))
baked_testing <- ifood_recipe |>
bake(testing(ifood_split))
# Fit a logistic regression.
fit <- logistic_reg() |>
set_engine("glm") |>
fit(Response ~ ., data = baked_training)
# Compute model accuracy.
fit |>
predict(new_data = baked_testing)|>
bind_cols(baked_testing) |>
accuracy(truth = Response, estimate = .pred_class)
fit |>
tidy(conf.int = TRUE)
